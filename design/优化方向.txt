

目标：实现推荐倾向的纠正，保留历史数据，附加一些额外的点赞、点踩功能

1.提供网页端进行控制
2.减少数据库占用，但是同时也希望保留一部分数据
3.减少内存占用，控制在200M左右
4.简化规则的使用，重新设计规则，提供说明文档
5.一天内的执行次数实际并不多，考虑减少缓存到内存
-- 继续完成基于现有数据大模型训练方向(不再考虑模型训练)


每个维度都要有黑白名单关键词（完成）
	标签要有，标题要有，封面也要有
	
	那么keyword这个类型应该就不需要了，因为有单独的
	检查枚举值
	
每个维度都要有忽略关键词（后端部分完成）（暂时取消，感觉没有意义，已经有精确匹配和复合匹配了）
	例如标签要有黑名单、白名单 对应的关键词

	增加字段is_ignore
	旧数据进行处理
	前端要新增对应的页面（未完成）
	
每个维度都要有一个缓存（后端部分完成）（暂时取消，感觉没啥用，训练没有意义的样子）
	增加字段is_cache（变成dict表的status字段对应 CACHE 的字典，这些字典就是暂存需要添加的）
	旧数据进行处理
	前端要新增对应的页面（未完成）
	
	
	
	
	
AiPrompt 这个类型的字典，是单个的还是多个
	目前是单条，不是多条
	


增加基于subtitle 字幕的关键字匹配（暂时取消）
完成基于ai的视频总结，前端界面提供模型和api-key的配置，允许用户进行开关，决定是否使用ai进行否决
	前端增加一个ai提示词编辑框，用于设定场景和判断条件：比如：你需要帮我判断xxx，这样的提示词
	
	
黑白名单页面应当统一，他两没有什么区别了（完成）
	先完善好黑名单的页面
	



	

黑名单-复合匹配（完成）
	新的表是 associate_rule
	白名单也使用此表
	
	
	这个表的规则不够全，要把什么标题，封面，简介等等所有可用规则都配上去
	


	创建规则
		输入描述，access_type 
	删除规则
		输入id
	
	添加接口
		就是普通的字典添加接口，但是要填充outer_id
		
	删除接口
		就是普通的字典删除接口，但是要填充 outer_id
	
	列表接口
		先找出规则，然后按照规则的id 对应 字典 outer_id，找出字典，
		把字典进行分类
	


	
黑白名单都应该把匹配模式改为：（完成）
	复合模式：多个规则匹配才匹配
	关键词模式：任意一个关键词匹配就算匹配
	
	
	同时使用两种规则，关键词可以放的很精准
	
	
	用“其他”类型的视频来校验这个规则
	
	


	
	



ai对文字进行定性（完成）

	已添加ai工具方法，并且找到了免费的glmflash，
	接下来就需要，把规则和ai的提示词结合起来。
	让用户输入ai的判断逻辑，然后发送黑白名单等规则，ai来输出是或否，从而增加黑白名单的判断。
	
	以前的关键字作为补充，只写一些敏感的，凡是匹配到敏感黑名单关键字统统pass，关键词不能匹配再到ai
	
	
	增加一种ai专用规则，其实是promote，用提示词作为规则，ai可以根据提示词，对照我们提供的视频信息，来实现判断
	

ai匹配，复合匹配，关键词匹配等，让用户来选择是否开启，不开启的情况下不匹配(完成)


video_detail 表的修改
	tag_ids 去除
		感觉没什么用，大部分视频都是空的，没有保存，到时候需要了再新增一张表
	last_modified_date删除
	
	增加handle_time
		就是第二次用户处理时的时间，用于图表展示
		
		旧数据设置为原本的last_modified_date
		
		
	去掉rcmd_reason
		目前都没有数据
		
	去掉pub_location
	
	去掉ctime，留pubdate就够了
	
	去掉 first_frame 
		有封面就行了
		
	增加 handle_step
		数字类型，未处理时是0，第一次机器处理是1，第二次处理是2，以此类推，处理完毕是100
		不允许为空。
		
		旧数据的handle_step设置如下：
			如果原本的 handle_type 不为空，且handle字段为0，则设置为1
			如果原本的 handle_type 不为空，且handle字段为1，则设置为100
			如果原本的 handle_type 为空，  则设置为0
		
		
	去掉handle 字段，
		handle_step 不等于 100 的都是未处理
		
	handle_reason 改为json类型（未匹配的规则不需要写入json）
		要不要拆开？为了动态性，不拆了
		
		{
			
			single_match:{
				is_match:true, //单一匹配成功则match标记为true
				
				//下面是具体匹配到了哪个关键词
				tag:[],
				desc:['舞蹈',...],
				title:[],
				cover:[],
				mid:[],
				tid:[],
				match_count: 3,// 匹配到几次关键词，单一包含 匹配任意一个即可
			},
			
			complex_match:{
				is_match:true,
				rule_name:'游戏规则1',//匹配到的那条规则的名称，不记录id，因为规则可以被删除
				tag:[],
				desc:['舞蹈',...],
				title:[],
				cover:[],
				mid:[],
				tid:[],
				match_count: 3,// 匹配到几次关键词，复合规则要>=3
			},
			
			
			ai_match:true, // ai判断的结果
			
			user_handle_reason:'',//用户处理结果，实际上只存放用户反转了判断等字符串
			
		}
		
		旧数据的处理，旧数据的handle_reason，设置为：
			{
				user_handle_reason:旧数据的原本handle_reason
			}



	tag怎么存
		用途：
			1.黑白名单时候的匹配
				主要用到tag的名字
			2.后续按照tag查找视频
			
			不在保留tag id
			不要video_tag 表，视频的tag直接用字符串，都好拼接，存储在video_detail里面
			
	
	
删除的表

	stat
	white_list_rule
	prepare_video
	video_reply
	video_relate
	video_tag
	watched_video
	
	
plus宏
	
	 count_by_condition
	
	并测试
	

select_page_by_condition 的宏


	

完成rust版本的视频判断（完成）

	黑名单匹配
	
	白名单匹配
	
关键词搜索任务（完成）
	
	api编写
	调用解析
	循环处理视频，挨个判断
	
	判断后打上标记（黑还是白， 处理状态 handle_type 是false，因为还没有到用户处理那一步），以及判断理由
	
	
	
热门排行榜任务（完成）

首页推荐任务（完成）	

二次处理

用户处理流程
	
	找出需要用户处理的数据
		应该有个处理状态字段，标记现在是进行了一次处理，还是二次处理，还是未经过任何处理
		
		保留一次处理的理由，和二次处理的理由
		
定时任务的执行
			
bili:mid 总是没有值，即使执行了check_accesskey

三次处理接口


凭证检测通过 需要在启动时执行一下，参考java版的处理

用户手动审核接口
	如果是other，直接变成100，因为不需要三次处理

修复fallback 未捕获问题

没有dev config为什么还报dev环境启动

启动后迁移文件未打包问题

鉴权问题
	
---------------------------------------------------------未完成的---------------------------------------------------------------





前端审核与纠正接口没有适配





对指定用户的视频进行点赞

总览接口编写	


	


	
ocr识别封面关键词
	
	封面部分的视频，需要本地部署ocr，部署到那个j1800吧
	


历史处理数据图表改为竖表
	纵轴为日期
	横轴为数量

	
提供一个批量查询字典的接口，而不是一个个轮训查






验证规则的效果，主要看  “视频审核-其他” 这种未匹配的数量多不多，
点赞，点踩的数量多不多




添加有理由的点踩（首页推荐底部有一个推荐理由列表 ）